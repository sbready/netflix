name some es6 other then const let arrow func
    Generators and iterators
    Spread and rest operators
    object.values

Active learning
    Front end masters
    Cloning Netflix

Node.js
    Docker 
    Microservices

Var name = ‘Steven ‘ + newDate().toDateString()

Return(
    <h1>{name</h1>
)

Filtering 
    Redux
    
    Const type = ’TYPE’

    Const reducer = ( action, state = {}) => {
        Switch( action.type) {
            Case type:
                Return {…state, action.payload}
            Break;

            Default: 
                Break;
        }
    }

Export action = (data) => {
    Return {
        Type,
        Payload: data
    }
}

Export {action, reducer as default }