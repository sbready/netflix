import React from 'react'
import StaticCarousel from '../StaticCarousel'
import MovieList from '../MovieList'

const Home = () => {
    return (
        <div>
            <StaticCarousel/>
            <MovieList/>
        </div>
    )
}

export default Home;
